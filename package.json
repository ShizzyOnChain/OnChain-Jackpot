{
  "name": "onchain-daily-jackpot",
  "version": "1.0.0",
  "description": "A premium twice-daily lottery on the blockchain.",
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=dist/main.js --jsx=automatic && cp index.html dist/index.html",
    "keeper": "ts-node scripts/settle.ts",
    "deploy": "ts-node scripts/deploy.ts"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ethers": "^6.13.1",
    "react": "^19.2.3",
    "react-dom": "^19.2.3"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3",
    "esbuild": "^0.23.0"
  }
}
